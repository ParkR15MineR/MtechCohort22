a Amazon website with basic function. images will not load because i did not care to add any
i'm pretty sure it uses local servers
/project-root
│
├── index.html               // Products page
├── checkout.html        // Checkout page
│
├── data/
│   └── products.js       // Product data array
│
├── cart/
│   └── cart.js               // Cart logic & shared state
│
├── pages/
│   ├── products.js       // Products page rendering & events
│   └── checkout.js      // Checkout page rendering & events
│
├── utils/
│   └── storage.js         // localStorage helpers (optional)
│
├── styles/
│   └── styles.css
│
└── README.md
pretty sure everything that the project asked for is in here