// in-class exercise 1
alertMessage(Fortnite);

function getWelcomeMessage(name) {
    alert('Welcome, ' + name + '!');
}

function divide(a, b) {
    return a / b;
}

function multiply(a, b) {
    return a * b;
}
